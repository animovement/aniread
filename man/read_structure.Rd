% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_structure.R
\name{read_structure}
\alias{read_structure}
\alias{read_structure_deeplabcut}
\alias{read_structure_sleap}
\title{Read a skeleton as an anistructure}
\usage{
read_structure(path, source = "auto")

read_structure_deeplabcut(path)

read_structure_sleap(path, skeleton = NULL)
}
\arguments{
\item{path}{Path to the file.}

\item{source}{\code{"auto"} to detect it from the file, or \code{"deeplabcut"} or
\code{"sleap"}.}

\item{skeleton}{For a \code{.slp} holding several skeletons, the name of the
one to read.}
}
\value{
An \code{\link[anicore:anistructure]{anicore::anistructure()}}.
}
\description{
Reads the skeleton a pose-estimation project defines — its keypoints and
the edges between them — as an \code{\link[anicore:anistructure]{anicore::anistructure()}} with points and
segments. Attach it to a frame with \code{\link[anicore:set_structure]{anicore::set_structure()}}.
\itemize{
\item \code{read_structure_deeplabcut()} reads a DeepLabCut project's
\code{config.yaml}: \code{bodyparts} (or, in a multi-animal project,
\code{multianimalbodyparts} and \code{uniquebodyparts}) and \code{skeleton}.
\item \code{read_structure_sleap()} reads a SLEAP \code{.slp} file (project or
predictions) or an analysis \code{.h5} export. Only body edges become
segments; SLEAP's symmetry edges pair left and right keypoints and are
not segments.
\item \code{read_structure()} detects which from the file, as \code{\link[=read_dataset]{read_dataset()}} does
for frames.
}

Edges keep the direction the file lists them in, which is what
\code{\link[anicore:as_anisegment]{anicore::as_anisegment()}} measures along.
}
\examples{
\dontrun{
skeleton <- read_structure("my-project/config.yaml")
af <- read_deeplabcut("my-project/videos/mouse1DLC.csv") |>
  anicore::set_structure(skeleton)
}
}
