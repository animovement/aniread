% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_motion.R
\name{read_frame_metrics_full}
\alias{read_frame_metrics_full}
\title{Full per-frame metrics via raw RGB pipe}
\usage{
read_frame_metrics_full(
  video_path,
  every_n_frames = 1L,
  pixel_subsample = 1,
  metrics = c("rgb", "luminance", "hsv", "activity", "spatial"),
  active_threshold = 10L,
  hwaccel = NULL,
  ffmpeg_path = "ffmpeg"
)
}
\arguments{
\item{video_path}{Path to the video file.}

\item{every_n_frames}{Integer. Extract every nth frame. Default \code{1L}.}

\item{pixel_subsample}{Numeric in \verb{(0, 1]}. Proportion of pixels sampled for
per-frame statistics. When less than 1, ffmpeg rescales the frame
(nearest-neighbour) rather than piping full-resolution data. Default \code{1.0}.}

\item{metrics}{Character vector of metric groups to compute. Any combination
of \code{"rgb"}, \code{"luminance"}, \code{"hsv"}, \code{"activity"}, and \code{"spatial"}. All
groups are computed by default.}

\item{active_threshold}{Integer (0--255). Luminance threshold defining
"active" pixels, used by \code{"activity"}, \code{"hsv"}, and \code{"spatial"} groups.
Default \code{10L}.}

\item{hwaccel}{Optional character string specifying an ffmpeg hardware
acceleration backend. Passed directly as \verb{-hwaccel <value>}. Common values:
\itemize{
\item \code{"cuda"} -- NVIDIA GPU (Windows/Linux). Requires a CUDA-capable GPU and
ffmpeg built with NVDEC support.
\item \code{"videotoolbox"} -- macOS (Apple Silicon benefits most; Intel Macs may
see no improvement or a slowdown due to GPU-to-CPU transfer cost).
\item \code{"vaapi"} -- Linux with Intel/AMD integrated graphics.
\item \code{"auto"} -- Let ffmpeg pick the best available backend.
}

Default \code{NULL} uses software decoding, which is reliable across all
platforms. Hardware acceleration can significantly speed up decode for
high-resolution or high-framerate video, but gains depend on the GPU,
codec, and filter chain. If the specified backend is unavailable, ffmpeg
silently falls back to software decode.}

\item{ffmpeg_path}{Path or command name for ffmpeg. Default \code{"ffmpeg"}
assumes it is on the system \code{PATH}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per extracted frame. \code{time}
contains the 1-based frame number, followed by columns for each requested
metric group as described in Details.
}
\description{
Extracts per-frame visual statistics by decoding raw RGB frames from ffmpeg
and computing per-channel summary statistics via a compiled C++ backend.
Returns a richer metric set than \code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}} including
distributional shape, spatial centroid, and activity counts.
}
\details{
Metric groups:
\itemize{
\item \strong{rgb}: Per-channel (R, G, B) statistics: mean, sd, median, min, max,
5th and 95th percentiles, skewness, excess kurtosis.
\item \strong{luminance}: The same nine statistics on \verb{0.299R + 0.587G + 0.114B}.
\item \strong{hsv}: Saturation mean/sd, value mean/sd, and circular hue statistics
(mean, concentration, sd) over pixels exceeding \code{active_threshold}.
\item \strong{activity}: Count and proportion of pixels above \code{active_threshold}.
\item \strong{spatial}: Luminance-weighted centroid (\code{mot_cx}, \code{mot_cy}) and weighted
spread (\code{mot_spread_x}, \code{mot_spread_y}) in original pixel coordinates.
}
}
\section{BehaveAI motion frames}{

BehaveAI's exponential colour-from-motion encoding maps temporal lags of
frame differences onto RGB channels: B captures the most recent motion
(t vs t-1), G the intermediate lag, and R the older motion trail. The
per-channel statistics returned here therefore carry direct temporal meaning.
For example, \code{r_mean} vs \code{b_mean} indicates whether motion is sustained or
newly initiated; \code{hue_concentration} from the \code{"hsv"} metric group reflects
how temporally consistent the motion pattern is within a frame.
}

\seealso{
\code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}}, \code{\link[=read_frame_metrics]{read_frame_metrics()}},
\code{\link[av:info]{av::av_video_info()}}
}
