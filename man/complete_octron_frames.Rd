% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_octron.R
\name{complete_octron_frames}
\alias{complete_octron_frames}
\title{Reinstate frames with no detection as all-NA rows}
\usage{
complete_octron_frames(data, frame_count)
}
\description{
Completes the full \code{track} × \code{frame} grid so that every analysed frame
appears for every track. Frames Octron dropped (because nothing was
detected) come back as rows that are NA in every measurement column.
The per-track class \code{label} is carried onto the reinstated rows so a
track keeps a consistent identity rather than gaining NA identity
values.
}
\details{
The frame range runs from 0 to \code{frame_count - 1} (the analysed-frame
count from the CSV header). When the header lacks that field the
observed \code{time} range is used instead. Frames present in the data but
beyond \code{frame_count} are preserved.
}
\keyword{internal}
