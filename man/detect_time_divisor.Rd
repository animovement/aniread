% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_trackball.R
\name{detect_time_divisor}
\alias{detect_time_divisor}
\title{Detect whether a numeric clock is in microseconds}
\usage{
detect_time_divisor(t)
}
\arguments{
\item{t}{Numeric timestamps.}
}
\value{
\code{1e6} for a microsecond clock, otherwise \code{1}.
}
\description{
A device clock in microseconds ticks in the thousands between samples, so the
median positive step separates it from a clock already in seconds. Zero-length
steps (duplicated timestamps) and \code{NA}s are excluded: with enough repeats the
plain median is 0, microseconds are never detected, and the resulting time
groups explode.
}
\keyword{internal}
