% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_source.R
\name{detect_source}
\alias{detect_source}
\title{Detect which source software wrote a file}
\usage{
detect_source(paths)
}
\arguments{
\item{paths}{Path to the file. If several paths are given (as
\code{\link[=read_trackball]{read_trackball()}} takes), only the first is inspected.}
}
\value{
The source name, as it appears in \code{\link[=get_supported_sources]{get_supported_sources()}}, or the
combined \code{"deeplabcut/lightningpose"}.
}
\description{
Works out which of the supported source software formats a file is in, by
looking at the file itself rather than trusting its name. This is what
\code{\link[=read_dataset]{read_dataset()}} uses when \code{source = "auto"}.

Candidates are first narrowed to the sources registered for the file's
suffix, then each candidate's detector inspects the file's contents. Exactly
one match is required.
}
\details{
DeepLabCut and LightningPose export structurally identical CSV files, so a
file in that format matches both. Rather than guess, \code{detect_source()}
returns the combined name \code{"deeplabcut/lightningpose"}; \code{\link[=read_dataset]{read_dataset()}}
reads it with \code{\link[=read_deeplabcut]{read_deeplabcut()}} - the parse is the same either way - and
records the combined name as the source, so the ambiguity is preserved
rather than silently resolved.

Detectors for HDF5, Parquet, XML and C3D files need an optional package
(\code{rhdf5}, \code{arrow}, \code{xml2}, \code{c3dr}). When one is not installed its sources
are skipped, and if nothing is detected the error names both the skipped
sources and the packages that would have been consulted.
}
\section{Writing a detector}{

A detector is a function of a single path returning \code{TRUE} when the file is
of that source. It must be cheap - read a handful of lines, or an HDF5
index, never the whole file - and it need not be defensive: \code{detect_source()}
runs each one with messages and warnings suppressed and treats an error as
"did not match".
}

\examples{
\dontrun{
detect_source("mouse.csv")
#> [1] "deeplabcut/lightningpose"
}
}
\seealso{
\code{\link[=read_dataset]{read_dataset()}} to read the file, \code{\link[=get_supported_sources]{get_supported_sources()}} for
the supported formats.
}
