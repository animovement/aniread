% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sample_data.R
\name{get_sample_data}
\alias{get_sample_data}
\title{Download sample tracking data}
\usage{
get_sample_data(source, cache_dir = tempdir())
}
\arguments{
\item{source}{Character string specifying the tracking software. Currently supported:
\itemize{
\item "animalta": Data from AnimalTA (single individual, multi-arena)
\item "anipose": Mouse paw tracking data
\item "bonsai": Tracking data from Bonsai
\item "deeplabcut": Mouse tracking from DeepLabCut
\item "fictrac": Fictrac sample data (.dat format)
\item "freemocap": FreeMoCap test data by frame
\item "idtrackerai": Trajectories from idtracker.ai (.h5 format)
\item "lightningpose": Mouse tracking from LightningPose
\item "sleap": Single mouse EPM tracking from SLEAP (.h5 format)
\item "trex": Beetle tracking from TRex
}}

\item{cache_dir}{Character string specifying the directory where to cache the downloaded
files. Defaults to a temporary directory using \code{tempdir()}. Set to a permanent
location to persist data across R sessions.}
}
\value{
Character string with the path to the downloaded file.
}
\description{
Downloads sample data for different animal tracking software and returns the path
to the downloaded file. The function caches the data to avoid repeated downloads.
}
\details{
The function downloads sample data from a GitHub repository and caches it locally.
If the file already exists in the cache directory, it will use the cached version
instead of downloading it again.

The data sources are hosted at: https://github.com/animovement/movement-data
}
\examples{
\dontrun{
# Get path to DeepLabCut sample data
path <- get_sample_data("deeplabcut")

# Read the data with the corresponding reader function
data <- read_deeplabcut(path)
}
}
