% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_boris.R
\name{backcalculate_boris_frames}
\alias{backcalculate_boris_frames}
\title{Recover frame numbers from timestamps when the image index is bad}
\usage{
backcalculate_boris_frames(start, stop, start_s, stop_s, fps)
}
\value{
A list with \code{start} and \code{stop} numeric vectors.
}
\description{
Some BORIS exports emit a bogus image index on boundary frames — e.g.
a STOP recorded on the very last frame of the video carries
\verb{Image index stop = 1} while its \code{Stop (s)} is the true end time. That
makes the frame-based \code{stop} smaller than \code{start}, which fails
aniframe's non-negative-interval invariant even though the
second-based timestamps are perfectly consistent.
}
\details{
BORIS derives \code{Time} from \code{frame / fps}, so when fps is known the true
frame number is \code{round(Time * fps)}. We only rewrite the rows whose
frame interval is negative \emph{and} whose second interval is
non-negative, leaving every other row on the verbatim image-index
values (which keeps the documented frame-alignment guarantee intact).
}
\keyword{internal}
