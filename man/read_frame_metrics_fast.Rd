% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_motion.R
\name{read_frame_metrics_fast}
\alias{read_frame_metrics_fast}
\title{Fast per-frame metrics via ffprobe signalstats}
\usage{
read_frame_metrics_fast(
  video_path,
  every_n_frames = 1L,
  ffprobe_path = "ffprobe"
)
}
\arguments{
\item{video_path}{Path to the video file.}

\item{every_n_frames}{Integer. Extract every nth frame. Default \code{1L}.}

\item{ffprobe_path}{Path or command name for ffprobe. Default \code{"ffprobe"}
assumes it is on the system \code{PATH}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per extracted frame. \code{time}
contains the 1-based frame number. Additional columns: \code{y_min}, \code{y_p10},
\code{y_mean}, \code{y_p90}, \code{y_max}, \code{sat_min}, \code{sat_mean}, \code{sat_max}, \code{hue_mean}.
Y and saturation values are in [0, 255]; hue in [0, 360).
}
\description{
Extracts per-frame luminance, saturation, and hue statistics by running
ffprobe's built-in \code{signalstats} filter. Because statistics are computed
inside ffprobe's own frame loop, no pixel data is transferred into R, making
this substantially faster than \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}} at the cost of a
reduced and YUV-based metric set.
}
\section{BehaveAI motion frames}{

On BehaveAI exponential motion frames the Y (luminance) channel captures
aggregate motion energy but collapses the temporal decomposition encoded
across R, G, and B. \code{hue_mean} provides a coarse proxy for the dominant
temporal phase of motion (i.e. whether activity is primarily recent or
trailing), and \code{sat_mean} reflects how temporally structured the motion is.
For full temporal channel decomposition use \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}.
}

\seealso{
\code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}, \code{\link[=read_frame_metrics]{read_frame_metrics()}}
}
