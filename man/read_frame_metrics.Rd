% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_motion.R
\name{read_frame_metrics}
\alias{read_frame_metrics}
\title{Read per-frame visual metrics from a video file}
\usage{
read_frame_metrics(
  video_path,
  every_n_frames = 1L,
  method = c("full", "fast"),
  pixel_subsample = 1,
  metrics = c("rgb", "luminance", "hsv", "activity", "spatial"),
  active_threshold = 10L,
  hwaccel = NULL,
  ffmpeg_path = "ffmpeg",
  ffprobe_path = "ffprobe"
)
}
\arguments{
\item{video_path}{Path to the video file.}

\item{every_n_frames}{Integer. Extract every nth frame. Default \code{1L}.}

\item{method}{One of \code{"full"} (default) or \code{"fast"}. See Details.}

\item{pixel_subsample}{Passed to \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}. Ignored when
\code{method = "fast"}.}

\item{metrics}{Passed to \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}. Ignored when
\code{method = "fast"}.}

\item{active_threshold}{Passed to \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}. Ignored when
\code{method = "fast"}.}

\item{hwaccel}{Passed to \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}. Ignored when
\code{method = "fast"}.}

\item{ffmpeg_path}{Path or command for ffmpeg. Passed to
\code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}.}

\item{ffprobe_path}{Path or command for ffprobe. Passed to
\code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per extracted frame. The \code{time}
column contains the 1-based frame number and can be used to join directly
with centroid tracking data from the same video. Remaining columns depend
on \code{method}; see \code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}} and
\code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}} for details.
}
\description{
A wrapper around \code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}} and \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}
that selects the appropriate implementation via the \code{method} argument.
}
\details{
The \code{"fast"} method delegates to ffprobe's built-in \code{signalstats} filter and
never moves pixel data into R, making it suitable for quick passes or very
long videos. The \code{"full"} method pipes raw RGB frames from ffmpeg into R and
computes a richer set of statistics.
}
\section{BehaveAI motion frames}{

BehaveAI's exponential colour-from-motion encoding maps temporal lags onto
RGB channels (B = recent, G = intermediate, R = older trail). On these
frames, the per-channel statistics returned by \code{method = "full"} carry direct
temporal meaning: e.g. a high R mean relative to B mean indicates sustained
rather than newly initiated motion. The \code{"fast"} method collapses this
temporal structure into YUV-based summaries and should be treated as a
coarse motion energy index only.
}

\seealso{
\code{\link[=read_frame_metrics_fast]{read_frame_metrics_fast()}}, \code{\link[=read_frame_metrics_full]{read_frame_metrics_full()}}
}
