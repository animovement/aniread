% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dataset.R
\name{read_dataset}
\alias{read_dataset}
\title{Read a movement or event dataset from any supported format}
\usage{
read_dataset(paths, source = "auto", ...)
}
\arguments{
\item{paths}{Path to the file to read. A few readers take more than one
path - \code{\link[=read_trackball]{read_trackball()}} takes one per sensor - in which case pass them
all; detection inspects the first.}

\item{source}{Which source software wrote the file. \code{"auto"} (the default)
detects it with \code{\link[=detect_source]{detect_source()}}. Otherwise one of the names in
\code{\link[=get_supported_sources]{get_supported_sources()}}.}

\item{...}{Passed on to the reader for \code{source}. This is how arguments that
only some readers take are supplied, e.g. \code{sampling_rate} for
\code{\link[=read_trackball]{read_trackball()}} or \code{path_probabilities} for \code{\link[=read_idtracker]{read_idtracker()}}.}
}
\value{
An \link[aniframe:aniframe]{aniframe} or
\link[aniframe:anievent]{anievent}, depending on the reader.
}
\description{
One entry point for every format \code{aniread} supports. By default the source
software is worked out from the file itself, so you do not have to know
which reader a file needs before opening it.
}
\details{
\code{read_dataset()} is a dispatcher, not a new reader: it works out which
reader to call and calls it. The object you get back is exactly what the
underlying reader returns - an \link[aniframe:aniframe]{aniframe} for tracking
data, or an \link[aniframe:anievent]{anievent} for behavioural events from
\code{\link[=read_boris]{read_boris()}}.

DeepLabCut and LightningPose CSV exports are structurally identical, so a
file in that format cannot be attributed to one or the other. Such a file is
read with \code{\link[=read_deeplabcut]{read_deeplabcut()}} - the parse is the same either way - and its
\code{source} metadata is set to \code{"deeplabcut/lightningpose"} to record that the
distinction is undetermined. Pass \code{source} explicitly to override this.
}
\examples{
\dontrun{
# Let aniread work out the format
data <- read_dataset("mouse.h5")

# Name it explicitly
data <- read_dataset("mouse.h5", source = "sleap")

# Reader-specific arguments pass straight through
data <- read_dataset(
  c("sensor1.csv", "sensor2.csv"),
  sampling_rate = 60,
  col_time = 4,
  col_dx = 1,
  col_dy = 2
)
}
}
\seealso{
\code{\link[=detect_source]{detect_source()}} to detect the format without reading,
\code{\link[=get_supported_sources]{get_supported_sources()}} for what is supported, and the individual
\verb{read_*()} functions for format-specific arguments.
}
