% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_fictrac.R
\name{read_fictrac}
\alias{read_fictrac}
\title{Read projected FicTrac data}
\usage{
read_fictrac(path, ball_radius = NULL, unit_ball_radius = "cm")
}
\arguments{
\item{path}{Character. Path to the FicTrac \verb{*.dat} file.}

\item{ball_radius}{Numeric (optional). Physical radius of the tracking ball.
When supplied the \code{x} and \code{y} coordinates are multiplied by this value.}

\item{unit_ball_radius}{Character. Unit of \code{ball_radius} (e.g., \code{"cm"} or
\code{"mm"}). Defaults to \code{"cm"}. Ignored when \code{ball_radius} is \code{NULL}.}
}
\value{
An anipoint with columns \code{time}, \code{x}, \code{y} and \code{heading}. Metadata
includes the source (\code{"fictrac"}), original filename, sampling rate,
time unit (\code{"s"}), space unit (either \code{"none"} or the value of
\code{unit_ball_radius}), and a Cartesian 2‑D coordinate system.
}
\description{
This helper loads a FicTrac \verb{*.dat} file, keeps the timestamp, the 2‑D
fictive path and the animal's heading, converts the timestamps to seconds,
and returns the result as an anipoint. The heading is declared as the
frame's orientation (\code{yaw}, in radians, from \code{x} toward \code{y}). FicTrac's
movement direction is the direction of travel, not orientation, and is not
kept. If the physical ball radius is supplied, the positions are scaled
accordingly and the spatial unit metadata is set.
}
\examples{
\dontrun{
# Assuming you have a FicTrac file called "fly1.dat"
traj <- read_fictrac("fly1.dat", ball_radius = 0.5, unit_ball_radius = "cm")
head(traj)
}

}
