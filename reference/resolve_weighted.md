# Compute the area-weighted mean across segments per row. Falls back to the arithmetic mean when areas are absent or sum to 0.

Compute the area-weighted mean across segments per row. Falls back to
the arithmetic mean when areas are absent or sum to 0.

## Usage

``` r
resolve_weighted(values_list, areas_list)
```
