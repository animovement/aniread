# Run one detector, treating any failure as a non-match

Run one detector, treating any failure as a non-match

## Usage

``` r
run_detector(detector, path)
```

## Arguments

- detector:

  A detector function.

- path:

  Path to the file.
