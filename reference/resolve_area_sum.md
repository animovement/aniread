# Sum all segment areas per row (used for `area` under `method = "weighted"`).

Sum all segment areas per row (used for `area` under
`method = "weighted"`).

## Usage

``` r
resolve_area_sum(areas_list)
```
