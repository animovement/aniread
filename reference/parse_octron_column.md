# Parse a column of mixed scalars and tuple-strings into a list of numeric vectors (one per row).

Parse a column of mixed scalars and tuple-strings into a list of numeric
vectors (one per row).

## Usage

``` r
parse_octron_column(col)
```
